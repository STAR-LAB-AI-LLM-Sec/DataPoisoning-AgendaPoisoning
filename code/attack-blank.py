def attack(env) -> None:
    """议程参会人注入（AttendeeInject）。

    参数
    ----
    env : 攻击环境对象，暴露 attacker_email 与 set_agenda(filename, content)。

    返回
    ----
    None。注入写入全部完成后，评测系统自动向 Agent 发送固定用户消息。
    """
    pass
